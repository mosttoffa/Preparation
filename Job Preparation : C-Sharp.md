## 
✅ <b>Async/Await (অ্যাসিঙ্ক/অ্যাওয়েট)  </b><br> 
Async/Await (অ্যাসিঙ্ক/অ্যাওয়েট) কী? <br> 
C# এ async/await হলো এমন feature যা দিয়ে কোড background এ non-blocking ভাবে কাজ করতে পারে, আর main thread বা UI/Request flow আটকে রাখে না। সহজভাবে, এটা দিয়ে আপনি slow কাজগুলোকে অপেক্ষা করাতে পারেন কিন্তু পুরো অ্যাপ বন্ধ না করে। <br> 
◼ কেন use হয়? <br> 
Fast response: Server request আটকে থাকে না.  UI freeze প্রতিরোধ: Web/UI thread stuck হয় না.  scalability: Enterprise app একসাথে বেশি request handle করতে পারে.  efficient resource use: Thread idle বসে wait করে না. <br> 
◼ কখন use হয়? <br> 
 Database query / complex stored procedure call.  File read/write.  API/HTTP call.  Image/Report/PDF generation.  Email/SMS send.  Realtime signal/event-based response processing.  <br> 
◼ কখন use করবেন? <br> 
যখন কাজটি সময় নেয়.  যখন আপনি অপেক্ষা করছেন (network, IO, DB, external service).  যখন controller থেকে response দ্রুত ফিরে দিতে চান, কিন্তু result পরে আসবে. <br> 
◼ Syntax / Structure : 
```
public async Task<ReturnType> MethodName(Parameter)
{
    // await দিয়ে slow task call করবেন
    ReturnType result = await SomeSlowTask();
    return result;
}
◼ Rules মনে রাখুন: 
Method এর আগে async keyword লাগে.  Slow task call এর আগে await লাগে.  Return type সাধারণত Task বা Task<T> হয়.  Calling side থেকেও await দিয়ে call করতে হয়. 
```
◼ LINQ তে async এর usecase : <br>
DB বা service থেকে list fetch করে LINQ run করা: <br> 
```
var list = await service.GetProductsAsync();
var result = list.Where(x => x.UnitPrice > 100).ToList();
```
<br> <br> 


---------------------------------------------------------------------------------------------------------------
✅<b>Delegates (ডেলিগেট) </b> <br>
Delegate (ডেলিগেট) কী? <br> 
C# এ delegate হলো method কে point করার জন্য একটি variable টাইপ, যেভাবে int এ সংখ্যা রাখা যায়, string এ লেখা রাখা যায়, ঠিক একইভাবে delegate এ method reference রাখা যায়। সহজ ভাষায়: Delegate = function pointer / callback container. এটি method call করে, method নিজে নয়, method এর reference ধরে. delegate == method এর reference রাখার container <br> 
◼ কেন ব্যবহার করা হয়? <br> 
callback system বানাতে (এক কাজ শেষ হলে আরেক কাজ চালাতে). event handling করতে. dynamic behavior inject করতে (conditionally method বদলানো). reusable execution pipeline তৈরিতে. higher order programming (method কে parameter হিসেবে পাঠানো) <br> 
◼ কখন ব্যবহার করবেন? <br> 
যখন আপনার দরকার: আপনার কোডে method আগে থেকেই জানা নেই. runtime এ সিদ্ধান্ত হবে কোন method চলবে. এক method থেকে অন্য method call trigger করতে হবে. component থেকে parent কে signal/data return করতে হবে. <br> 
◼ <b>ডেলিগেট (Delegate)-এর প্রধান বিকল্পগুলো হলো বিল্ট-ইন জেনেরিক ডেলিগেট (Func, Action, Predicate) এবং ল্যাম্বডা এক্সপ্রেশন (Lambda Expressions) ও ইন্টারফেস (Interface)। </b> <br> 

আধুনিক C# কোডে, বিশেষ করে .NET Core বা .NET 5+ এ, সাধারণত কাস্টম ডেলিগেট (Custom Delegate) তৈরি করা হয় না। পরিবর্তে, এই বিকল্পগুলি ব্যবহার করা হয়: <br> 
💡 Delegate-এর মূল বিকল্পসমূহ (Key Alternatives to Custom Delegates) 
বিল্ট-ইন জেনেরিক ডেলিগেট (Built-in Generic Delegates): <br> 
Func, Action, এবং Predicate ব্যবহার করে আপনি বেশিরভাগ ডেলিগেট ব্যবহারের প্রয়োজনীয়তা পূরণ করতে পারেন। এগুলি ব্যবহার করলে আপনার কোড আরও সংক্ষিপ্ত, পঠনযোগ্য (readable) এবং ইন্ডাস্ট্রি স্ট্যান্ডার্ডের হয়। 
```
Alternative     |      	রিটার্ন টাইপ (Return Type)	    |     প্যারামিটার (Parameters)	   |     কখন ব্যবহার করবেন? (When to Use?)
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Action	         |   void (কিছুই রিটার্ন করে না)	        |  ০ থেকে ১৬টি	                 |  যখন আপনি একটি মেথডকে কল করতে চান যা কোনো কাজ করবে, কিন্তু কোনো মান ফেরত দেবে না। (যেমন: কনসোলে কিছু প্রিন্ট করা, একটি ইভেন্ট ট্রিগার করা)।
Func	           |  TResult (যে কোনো ডেটা টাইপ)	     |   ০ থেকে ১৬টি	                |  যখন আপনি একটি মেথডকে কল করতে চান যা কিছু মান ফেরত দেবে। এটি ক্যালকুলেশন বা ডেটা রূপান্তরের জন্য সবচেয়ে বেশি ব্যবহৃত হয়।
Predicate	      |  bool	                             |   ১টি	                        |  যখন আপনার কোনো শর্ত বা ফিল্টারিং লজিক প্রয়োজন হয় যা একটি বুলিয়ান মান (True/False) রিটার্ন করবে। (যেমন: লিস্ট থেকে জোড় সংখ্যা খুঁজে বের করা)।
```
আধুনিক C# এ, কাস্টম ডেলিগেট তৈরি না করে Func বা Action ব্যবহার করা, এবং প্রয়োজন অনুযায়ী ইন্টারফেস ব্যবহার করাই সবচেয়ে ভালো প্র্যাকটিস।
<br> <br>  <br> 


---------------------------------------------------------------------------------
✅<b>LINQ (Language Integrated Query) </b> <br> 
LINQ (Language Integrated Query) হলো C# এর data query technology, যেটা দিয়ে collections, databases, XML, APIs বা in-memory lists থেকে filter/search/sort/group/join করে data বের করা যায় type-safe ও readable ভাবে। সহজ কথায়: LINQ allows you to write SQL-like queries directly within your C# code. <br>  
◼ কেন ব্যবহার করা হয়? <br> 
Data filtering & transformation সহজ ও নিরাপদ করতে. SQL-like query direct C# code এ লেখার সুবিধা দিতে. Database query short + optimized বানাতে. No manual loops for basic query logic. Entity Framework Core + Repository Pattern এ standard querying layer.  <br> 
◼ কখন ব্যবহার করবেন? <br> 
Real scenarios: Controller এ database থেকে filtered product list আনা. BLL এ business logic অনুযায়ী data process করা. ViewModel বানানোর আগে data transform করা. Reports page এ date/category/customer অনুযায়ী records বের করা. Duplicate remove বা unique list বানানো  <br> 
◼ কিভাবে ব্যবহার করা হয়? <br> 
2 ধরনের syntax: 1.Query Syntax (looks like SQL). 2.Method Syntax (extension method based) <br> 
Data source types: List<T>, array, Dictionary, DB Context, APIs etc. <br> 
Syntax / structure overview: <br> 
```
◼ Query Syntax :

    from item in dataSource
    where condition
    orderby item.Property
    select item;
```
```
◼ Method Syntax :

    dataSource.Where(x => condition)
              .OrderBy(x => x.Property)
              .Select(x => x);
```
◼ Key Operators: 

| Operator            | Purpose           |
| ------------------- | ----------------- |
| `Where`             | Filter            |
| `Select`            | Project/transform |
| `OrderBy / ThenBy`  | Sort              |
| `GroupBy`           | Aggregation       |
| `Join`              | Relational link   |
| `FirstOrDefault`    | Safe single fetch |
| `Distinct`          | Unique list       |
| `Any / All`         | Condition check   |
| `Count / Sum / Avg` | Math ops          |

<br> <br>  <br> 

-----------------------------------------------------------------------------------------------
✅<b>Garbage Collector (GC)</b> <br> 
◼Garbage Collector (GC) কিভাবে কাজ করে? <br> 
সংজ্ঞা (Definition):
Garbage Collector (GC) হলো .NET Framework বা .NET Runtime-এর একটি built-in memory management system, যা স্বয়ংক্রিয়ভাবে অপ্রয়োজনীয় বা অব্যবহৃত object গুলোকে memory থেকে মুছে ফেলে। <br> 
C#-এর garbage collector মূলত নিচের ৩-ধাপের classic algorithm-এর উপর ভিত্তি করে কাজ করে, যেটাকে বলা হয় <br> 
    Mark–and–Sweep–Compact Algorithm (Generational GC version) <br> 
◼ Garbage Collection = ৩টি ধাপে কাজ করে- <br> 
1️⃣ Mark Phase → কে কে এখনো বেঁচে আছে (alive / reachable object) তা খুঁজে বের করে <br> 
2️⃣ Sweep Phase → বাকি মৃত (unreachable) object গুলোর memory ফাঁকা করে <br> 
3️⃣ Compact Phase → ফাঁকা জায়গাগুলো সরিয়ে memory contiguous করে <br> 

<br> <br> <br> 


-----------------------------------------------------------------------------------------------
✅ <b>জেনেরিক কালেকশন (Generic Collections)  </b> <br>  
জেনেরিক কালেকশনগুলি আধুনিক C# (সাধারণত .NET 2.0 থেকে) এর অংশ এবং এগুলি System.Collections.Generic নেমস্পেস ব্যবহার করে। ASP.NET সহ যেকোনো আধুনিক C# ডেভেলপমেন্টে এগুলিই বেশি ব্যবহৃত হয়। <br> 
◼প্রধান বৈশিষ্ট্য: <br> 
```
টাইপ সেফটি (Type Safety): কালেকশনটি তৈরি করার সময়ই আপনি নির্দিষ্ট করে দেন যে এটি কোন ধরনের ডেটা সংরক্ষণ করবে (যেমন: List<int>)। কম্পাইলার নিশ্চিত করে যে ভুল টাইপের ডেটা যেন কালেকশনে প্রবেশ করতে না পারে।  <br>
বক্সিং/আনবক্সিং এড়িয়ে চলা: যেহেতু টাইপটি নির্দিষ্ট করা থাকে, তাই ভ্যালু টাইপগুলি object টাইপে রূপান্তরিত হয় না। এর ফলে কোনো বক্সিং বা আনবক্সিং হয় না। <br> 
    সুবিধা: এটি কর্মক্ষমতা (Performance) বাড়ায় এবং মেমরি ব্যবহার কমায়। <br> 
কম্পাইলটাইম ত্রুটি (Compile-time Errors): ডেটা টাইপ সংক্রান্ত যেকোনো ত্রুটি রানটাইমের পরিবর্তে কম্পাইল করার সময়ই ধরা পড়ে। <br> 
```
উদাহরণ:  
```
  কালেকশন ক্লাস                                        বর্ণনা
---------------------------------------------------------------------------------------------------------------
List<T>                           জেনেরিক ArrayList-এর সমতুল্য। সর্বাধিক ব্যবহৃত কালেকশন। T হলো টাইপ প্লেসহোল্ডার।
"Dictionary<TKey, TValue>"        জেনেরিক Hashtable-এর সমতুল্য। দ্রুত ডেটা অনুসন্ধানের জন্য ব্যবহৃত হয়।
Stack<T>                          জেনেরিক LIFO কালেকশন।
Queue<T>                          জেনেরিক FIFO কালেকশন।
HashSet<T>                        ডেটার ডুপ্লিকেট (Duplicate) এড়িয়ে শুধুমাত্র অনন্য (Unique) আইটেম সংরক্ষণ করার জন্য।
```
🆚 সারসংক্ষেপ: পার্থক্য 
```
  বৈশিষ্ট্য                        নন-জেনেরিক কালেকশন (যেমন: ArrayList)                    জেনেরিক কালেকশন (যেমন: List<int>)
প্রধান টাইপ            object                                                    <T> (নির্দিষ্ট টাইপ)
টাইপ সেফটি            নেই (No Type Safety)                                      আছে (Type Safe)
কর্মক্ষমতা              ধীরগতিসম্পন্ন (Boxing/Unboxing-এর কারণে)                    দ্রুতগতিসম্পন্ন (Boxing/Unboxing হয় না)
ব্যবহৃত নেমস্পেস        System.Collections                                        System.Collections.Generic
ব্যবহারের সুপারিশ       আধুনিক C# প্রোজেক্টে এড়িয়ে চলা উচিত।                         আধুনিক C# প্রোজেক্টে সর্বদা ব্যবহার করা উচিত।
```
◼ মূল জেনেরিক কালেকশন:
```
   * List<T>
   * Dictionary<TKey, TValue>
   * HashSet<T>
   * Queue<T>
   * Stack<T>
```
<br> <br> <br> 


------------------------------------------------------------------------------------------------
✅ <b>List vs. Array </b>  
Example Answer (উদাহরণমূলক উত্তর) <br> 
"আমি অ্যারে (Array) তখনই ব্যবহার করি যখন আমি নিশ্চিত যে আমার কালেকশনের আকার প্রোগ্রাম চলার সময় পরিবর্তন হবে না (যেমন: মাসের নাম বা রঙের কোড)। অ্যারে মেমরিতে পরপর অবস্থান করে, তাই এটি List<T> এর চেয়ে দ্রুত অ্যাক্সেস দেয় এবং মেমরি ফুটপ্রিন্টও কম। <br> 

অন্যদিকে, আমি List<T> ব্যবহার করি বেশিরভাগ সময়। বিশেষ করে যখন ব্যবহারকারীর ইনপুট বা ডেটাবেস থেকে ডেটা লোড হয়, তখন কালেকশনের আকার আগে থেকে জানা থাকে না। List<T> ডেটা যোগ করা (Add) এবং অপসারণ করার জন্য অনেক সহজ এবং এর ডাইনামিক আকারের কারণে মেমরি ম্যানেজমেন্ট নিয়ে চিন্তা করতে হয় না। লিস্টের জেনেরিক ব্যবহার নিশ্চিত করে যে আমি টাইপ-সেফ কোড লিখছি।" <br> 


<br> <br> <br> 


-----------------------------------------------------------------------------------------------







